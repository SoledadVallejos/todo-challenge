# todo-challenge

Instalar las dependencias:

    ```bash
    npm install
    # o
    yarn install
    # o
    pnpm install
    ```

  Ejecutar la aplicación:

    ```bash
    npm run dev
    # o
    yarn dev
    # o
    pnpm dev
    ```

